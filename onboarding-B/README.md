# Onboarding B Task
In this directory you can find an iron-base repository to use for going through the examples + evaluations

When following along with the exammples, make sure you open the examples directory in vscode and utilize that. Use the evaluations directory when doing every eval.